{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run SyscomScript",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/syscom.py",
      "args": ["${file}"],
      "console": "integratedTerminal",
      "cwd": "${workspaceFolder}"
    }
  ]
}
