{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run SyscomScript Extension",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}"
      ],
      "outFiles": [
        "${workspaceFolder}/**/*.js"
      ],
      "preLaunchTask": "npm: install"
    }
  ]
}